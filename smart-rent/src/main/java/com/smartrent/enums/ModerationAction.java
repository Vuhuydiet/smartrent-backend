package com.smartrent.enums;

/**
 * Actions that can be performed in the moderation lifecycle.
 */
public enum ModerationAction {
    APPROVE,
    REJECT,
    REQUEST_REVISION,
    RESUBMIT,
    SUSPEND
}
